<div class="container">
    <h1>Edit profile</h1>
        <%= form_with(model: @user, local: true) do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name, class: 'form-control' %>
        <%= f.file_field :avatar %>
        <%= f.submit "Submit" %>
    <% end %>
</div>